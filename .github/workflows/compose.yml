services:
  windows:
    image: dockurr/windows
    container_name: windows
    environment:
      DISK_SIZE: "62G"
      RAM_SIZE: "13G"
      CPU_CORES: "4"
      DISK_TYPE: "ide"
      VERSION: "11"
      environment:
      ARGUMENTS: "-drive file=/driver.iso,media=cdrom -drive file=/vb.iso,media=cdrom"
    devices:
      - /dev/kvm
    cap_add:
      - NET_ADMIN
    ports:
      - 8006:8006
      - 5900:5900/tcp
      - 5900:5900/udp
      - 3389:3389/tcp
      - 3389:3389/udp
    volumes:
      - /mnt:/storage
      - /isos/driver.iso:/driver.iso
      - /isos/vb.iso:/vb.iso
    stop_grace_period: 2m
